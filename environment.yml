name: cac_np2
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - pymarc
      - streamlit-aggrid
      - numpy==2.*
      - pandas
      - pytest
      - streamlit>=1.30
      - python-dotenv
      - pillow
      - s3fs
      - matplotlib
prefix: C:\Users\HLloyd\AppData\Local\mambaforge\envs\cac
